{
  "name": "analytics-reporter",
  "version": "1.0.0",
  "description": "Analytics reporter microservice for the URL Shortener — analytics-reporter service in monorepo",
  "main": "index.js",
  "scripts": {
    "start"  : "node index.js",
    "dev"    : "nodemon index.js",
    "test"   : "jest --coverage",
    "lint"   : "node --check index.js"
  },
  "dependencies": {
    "axios"  : "^1.6.7",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest"         : "^29.7.0",
    "nodemon"      : "^3.0.3",
    "supertest"    : "^6.3.4"
  }
}
